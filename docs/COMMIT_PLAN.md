# Realistic Commit Plan (TDD)

1. chore: project setup and tooling
2. test(auth): failing tests for registration
3. feat(auth): implement registration
4. test(auth): failing tests for login
5. feat(auth): implement login + JWT
6. refactor(auth): extract auth service

7. test(sweets): failing tests for CRUD
8. feat(sweets): implement sweets CRUD
9. refactor(sweets): validation and DTO cleanup

10. test(inventory): failing tests for purchase/restock
11. feat(inventory): implement inventory logic

12. test(authz): role-based access tests
13. feat(authz): enforce admin-only routes

14. chore: docs and README